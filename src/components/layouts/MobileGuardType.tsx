export type MobileGuardType = {
  children: React.ReactNode;
  spacesFromEdges: SpacesFromEdgesType;
};

export type SpacesFromEdgesType = {
  top: number;
  bottom: number;
  left: number;
  right: number;
};
